node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
uploads/

# Secrets — never commit
.env
.env.*
!.env.example
*.dump

# Separate nested service (has its own git repo)
OwlTesterService/
